# MCP Server (legacy path)

The MCP server has moved to [`packages/mcp-server/`](../packages/mcp-server/).

Build and run from the repo root:

```bash
npm ci && npm run build
npm start
# or: npx stock-buddy-mcp
```
